<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.training.core.mapper.TrainingUserMapper">
    <insert id="addPerson">
        insert into training_user(training_id,user_id,is_del,create_time,update_time,create_user_id,status)
        values(#{trainingId},#{userId},0,now(),now(),#{create_id},100)
    </insert>

    <select id="cancelSignUp" resultType="Integer">
        update training_user set is_del='1',update_time=now() where training_id=#{trainingId} and user_id=#{user}
    </select>

    <select id="getTrainingUserById" resultType="Integer">
        select is_del
        from training_user where training_id=#{trainingId} and user_id=#{userId}
    </select>

    <update id="updatePerson" >
        UPDATE training_user SET is_del = 0 WHERE training_id=#{trainingId} AND user_id=#{userId}
    </update>
</mapper>
