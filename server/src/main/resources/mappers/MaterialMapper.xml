<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.training.core.mapper.MaterialMapper">
<select id="findMaterialList" resultType="com.example.training.core.entity.vo.MaterialListVO">
    select material_id,material_name,material_type,user_id,file_id,file_status,is_del,create_time,update_time
     from material
     where is_del = 0
    <if test="limit != null and offset != null">
        limit #{limit} offset #{offset}
    </if>
</select>
</mapper>
